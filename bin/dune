(executable
  (public_name plum)
  (libraries plum_core)
  (modules plum ast grammar lexer parser))

(menhir
  (modules grammar))

(ocamllex
  (modules lexer))

(env
  (dev
    (flags (:standard -w -A))))